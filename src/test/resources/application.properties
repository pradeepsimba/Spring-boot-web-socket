# Test context-load only: no real Postgres is required as long as HistoricalDataRepository
# is mocked in the test (see AlgosocketApplicationTests) so no eager connection is attempted.
spring.datasource.url=jdbc:postgresql://localhost:5432/algo_test
spring.datasource.username=test
spring.datasource.password=test
